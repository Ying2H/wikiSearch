# Runtime dependencies: none.
# The crawler, SQLite state layer and parsers currently use Python's standard library.
# Keep this file so a server bootstrap can continue to use `pip install -r requirements.txt`.
